# CLAUDE.md

## アプリケーション概要
- 未着手 / 進行中 / 完了 の3列で構成されるシンプルなカンバン型タスク管理アプリ。
- タスクを作成し、列間をドラッグ&ドロップで移動させることでステータスを管理する。
- 主な機能:
    - カンバンボードの表示
        - 未着手 / 進行中 / 完了 の3列を横に並べて表示する。
        - 各列には、その列に属するタスクをカード形式で一覧表示する。
        - 各タスクカードにはタイトルと説明を表示する。
    - タスクの追加
        - ユーザーはタイトルと説明を入力してタスクを新規作成できる。
        - 新規作成したタスクは「未着手」列に追加される。
        - タイトルは必須項目とする(空のタスクは作成できない)。
    - ドラッグ&ドロップによる列移動
        - タスクカードをドラッグし、別の列にドロップすると、そのタスクのステータス(所属する列)が更新される。
        - ドロップ後は移動先の列にタスクが表示され、移動元の列からは消える。
- スコープ外(現時点は対象としない)
    - ユーザー認証・複数ユーザーでの共有
    - 複数ボードの作成・切り替え
    - タスクの編集・削除
    - 期限日・担当者・優先度などの付加属性
    - 列内でのタスクの並び替え

## 技術スタック

- 言語: TypeScript
- フレームワーク: Next.js(App Router) — `create-next-app` で構成
- スタイリング: Tailwind CSS
- ビルド/開発サーバー: Turbopack
- テスト: Vitest(ユニットテスト)
- ソース配置: `src/` 配下
- コンポーネント方針: 責務ごとに分割する(例: ボード全体・列・タスクカード・追加フォームを別コンポーネントにする)

## Git操作について

- git操作を行う際は `gh` コマンドを使用してください。

### ブランチルール

- デフォルトブランチ: main
- ブランチ命名規則:
    - 機能追加: feature/#Issue番号
    - バグ修正: fix/#Issue番号
    - リファクタリング: refactor/#Issue番号
    - ドキュメント: doc/#Issue番号
    - 検証・実験: poc/#Issue番号
- main への直接コミットは原則禁止し、ブランチ経由でマージする

### コミットルール

- 1つのコミットに複数の目的を混在させず、目的ごとにコミットを分割してください。
- コミットメッセージには絵文字を付与してください（絵文字コミット）。
    - `絵文字 コミット内容` のフォーマットでコミットを行ってください
- 絵文字にはコミットの内容に応じて以下を使い分けてください
    | 絵文字 | 意味 |
    | --- | --- |
    | 🐛 | バグ修正 |
    | 👍 | 機能改善 |
    | ✨ | 部分的な機能追加 |
    | 🎉 | 大きな機能追加 |
    | ♻️ | リファクタリング |
    | 🚿 | 不要な機能・使われなくなった機能の削除 |
    | 💚 | テストやCIの修正・改善 |
    | 👕 | Lintエラーやコードスタイルの修正 |
    | 🚀 | パフォーマンス改善 |
    | 🆙 | 依存パッケージなどのアップデート |
    | 🔒 | 新機能の公開範囲の制限 |
    | 👮 | セキュリティ関連の改善 |
    | 📝 | ドキュメントの作成・修正 |
